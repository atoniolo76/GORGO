# Source drafts (archived)

These markdown drafts were pulled from the `main` branch and are the source
material for the consolidated `research/paper.tex`:

- `paper_results_draft.md` — full Results working draft (3-weight, p50/p95, the
  2D cost model, decoded-trace and calibrated experiments, the apr7 load sweep,
  and the reward-hacking / E2E / PD-disaggregation discussion). The substantive
  final-result content (2D model + load sweep + saturation discussion) has been
  integrated into `paper.tex` §5.4 (2D Cost Model and Load Sensitivity) and the
  Discussion; dead-end design iterations marked "Not in the final paper" were
  deliberately left out.
- `intro_costmodel_draft.md` — Introduction + Cost Model working draft.

`paper.tex` is canonical; these are kept for provenance only.
